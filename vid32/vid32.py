# shutil performs file operations like mv and cp

# important methods are:
# copy - which copies a file
# move - which moves a file
# move - can also be used to rename a file
# copytree - which copies an entire directory's contents

import shutil

